name: Build Burri Protect

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            ninja-build \
            cmake \
            git \
            zip \
            unzip \
            wget \
            python3

      - name: Install Android NDK r29
        run: |
          wget -q https://dl.google.com/android/repository/android-ndk-r29-linux.zip
          unzip -q android-ndk-r29-linux.zip
          mv android-ndk-r29 $HOME/android-ndk-r29

      - name: Set NDK environment
        run: |
          echo "ANDROID_NDK_HOME=$HOME/android-ndk-r29" >> $GITHUB_ENV
          echo "ANDROID_NDK_ROOT=$HOME/android-ndk-r29" >> $GITHUB_ENV
          echo "NDK_HOME=$HOME/android-ndk-r29" >> $GITHUB_ENV
          echo "$HOME/android-ndk-r29/toolchains/llvm/prebuilt/linux-x86_64/bin" >> $GITHUB_PATH

      - name: Build
        run: |
          if [ -f build.sh ]; then
            chmod +x build.sh
            ./build.sh
          else
            echo "build.sh not found; skipping build step."
          fi

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: burri_protect
          path: |
            **/*.so
            **/*.a
            **/*.bin
            **/build/**
